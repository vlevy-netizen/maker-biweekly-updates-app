{
  "name": "slack-maker-update-bot",
  "version": "1.0.0",
  "description": "AWS Lambda Slack bot for biweekly project updates",
  "main": "lambda_handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name slack-maker-update-bot --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.1",
    "dayjs": "^1.11.10",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "slack",
    "bot",
    "lambda",
    "aws",
    "project-management"
  ],
  "author": "Your Name",
  "license": "MIT"
}
